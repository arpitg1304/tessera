# Tessera Environment Variables

# Backend Configuration
STORAGE_PATH=/var/tessera/storage
DATABASE_PATH=/var/tessera/tessera.db
TESSERA_BASE_PATH=/var/tessera

# Admin Panel (CHANGE THIS IN PRODUCTION!)
ADMIN_PASSWORD=change-me-in-production

# For local development, use:
# STORAGE_PATH=./data/storage
# DATABASE_PATH=./data/tessera.db
# TESSERA_BASE_PATH=./data

# Optional: Domain configuration (for production with reverse proxy)
# DOMAIN=tessera.yourdomain.com
